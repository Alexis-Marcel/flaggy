# Server
FLAGGY_PORT=:8080
FLAGGY_DB_PATH=flaggy.db

# CLI — used by bin/flaggy to reach the server
FLAGGY_SERVER=http://localhost:8080

# Auth — if unset, auth is disabled (dev mode)
FLAGGY_MASTER_KEY=

# Server
FLAGGY_PORT=:8080
FLAGGY_DB_PATH=flaggy.db

# CORS — set to "false" to disable (when behind a reverse proxy like Oathkeeper)
FLAGGY_CORS=true

# Auth — if unset, auth is disabled (dev mode)
FLAGGY_MASTER_KEY=
